# Deployment

## Prerequites
- Repository must be public
- Repository pages settings must be set with "Github Actions" source
- Root dir in vite.config.ts must be match the repository name

## Pipeline
The deployment pipeline is automatically trigger when push to main branch.
